// Публичный API этого модуля ещё не используется: раннер, снапшот/дифф и
// команды подключат его в задачах 4, 5 и 7. До тех пор компилятор считает
// каждый pub-элемент мёртвым кодом — allow снимается по мере подключения.
#![allow(dead_code)]

pub mod bd;
pub mod model;
